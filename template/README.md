 # Template

이 폴더는 간단한 `Makefile` 템플릿과 예제 파일을 모아둔 곳입니다. 새로운 연습 문제를 만들 때 이 템플릿을 복사하여 사용하면 빌드 구성이 편리합니다.

예시 사용법

```powershell
cd template
make        # 템플릿이 준비된 경우 빌드
```

템플릿에 포함시키면 좋은 항목

- `Makefile` : 빌드/클린 규칙
- `src/` : 소스 코드 위치
- `include/` : 헤더 파일 위치
- `tests/` : 간단한 테스트 스크립트
